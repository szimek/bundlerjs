const install = require('../npm-commands/install');
const prune = require('../npm-commands/prune');
const shrinkwrap = require('../npm-commands/shrinkwrap');

module.exports = {
  install,
  prune,
  shrinkwrap,
};
