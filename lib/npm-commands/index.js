const install = require('../npm-commands/install');
const outdated = require('../npm-commands/outdated');
const prune = require('../npm-commands/prune');
const shrinkwrap = require('../npm-commands/shrinkwrap');
const update = require('../npm-commands/update');

module.exports = {
  install,
  outdated,
  prune,
  shrinkwrap,
  update,
};
